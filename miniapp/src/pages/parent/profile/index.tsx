import { View, Text } from '@tarojs/components'
import styles from './index.module.scss'

export default function ParentProfilePage() {
  return (
    <View className={styles.container}>
      <Text className={styles.text}>⚙️ 设置 - Phase 6 开发中</Text>
    </View>
  )
}
